[package]
name = "rtt-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
rtt-core = { path = "../rtt-core" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
